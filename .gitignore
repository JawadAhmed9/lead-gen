# secrets
.env

# python
__pycache__/
*.pyc
venv/
.venv/

# node / frontend build
node_modules/
ui/node_modules/
ui/dist/

# os / misc
.DS_Store
*.log

# runtime rotation state (regenerated from settings on first run)
rotation_profiles.json

# sqlite WAL sidecar files (runtime only — never commit)
data/*.db-wal
data/*.db-shm

# stray local test scripts
_w3_test.py
_*_test.py

# local UI preview (not part of the app)
login_preview.html

# NOTE: data/leads.db is intentionally tracked — it's the baseline dataset the
# app ships with. Keep this repo PRIVATE (it contains real contact info).
mobile_preview.html
